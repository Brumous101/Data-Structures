#pragma once
#include <array_list.h>
#include <linked_list.h>
#include <queue.h>
#include <sorted_array_list.h>
#include <stack.h>

// This looks silly, but it gives me a common place to put the ssuds / doxygen documentation

/// S.S.U.D.S. (SSU Data Structures) is our data structures implementation of common data
/// structures.  In general, these classes are probably less good than the STL equivalents
/// (vectors > ArrayList, linked_list > LinkedList, etc.).  There are a few data structures
/// that don't (currently) have a STL equivalent.
namespace ssuds
{

}
